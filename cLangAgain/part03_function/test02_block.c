#include <stdio.h>

/* :::本地变量::: 又叫 局部变量、自动变量， 是定义在块内的 */
/* 块可以是函数的块，也可以是语句的块，如 if, 甚至随便拉一对大括号，块外面的变量仍然可以访问 */


/* :::变量的生存期::: 啥时候出现和消亡 */

/* :::变量的作用域::: 啥地方可以访问这 */

/* 对于本地变量，生存期和作用域 答案唯一 块（大括号内） */

/* 不在作用域内不代表不在生存期内 */

int main()
{
  
  return 0;
}