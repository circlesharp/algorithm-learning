#include <stdio.h>

typedef struct {
  int x;
  int y;
  int quadrant;
} point;

typedef int base64_i;

int main()
{
  point p = {1, 2, 1}; 
  point arr[10]; // 有10个 point 结构变量的数组
  return 0;
}